#@ load("rabbitmq.lib.yml", "name", "username", "password")

apiVersion: v1
kind: Secret
metadata:
  name: #@ name()
  labels:
    app: #@ name()
data:
  rabbitmq-user: #@ username()
  rabbitmq-password: #@ password()
